# Leave empty to use same-origin (Vite proxy locally, or Docker / reverse proxy).
# For a split deploy, point at the API:
# VITE_API_URL=https://your-api.onrender.com
VITE_API_URL=http://localhost:8000
