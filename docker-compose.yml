services:
  app:
    build: .
    ports:
      - "8000:10000"
    environment:
      PORT: "10000"
      CORS_ORIGINS: "*"
      DATA_DIR: /app/data
    volumes:
      - face-static:/app/static
      - face-db:/app/data

volumes:
  face-static:
  face-db:
